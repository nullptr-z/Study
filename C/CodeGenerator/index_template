import React, { Component } from 'react';
import { connect } from 'dva';

class $1 extends Component {
  render() {
    return (
        <div></div>
    );
  }
}

export default connect(({ loading, $2 }) => {
  return {
    loading: loading.models.$2,
  };
})($1);